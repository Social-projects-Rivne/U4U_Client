import React from 'react';
import './CommentAuthorName.scss'

  const CommentAuthorName = (props) => {
    
        return  (
            <p className ='CommentAuthorName'>{props.name}</p>
                 
        )
    }
 export default CommentAuthorName;
